#!/usr/bin/env bash
# Function to display information about a subdomain

display_subdomain_info() {
    local domain="$1"
    local subdomain="$2"

    if [ -n "$subdomain" ]; then
        local record_type=$(dig +short "$subdomain.$domain" | awk '/IN/ {print $4}')
        local destination=$(dig +short "$subdomain.$domain" | awk '/IN/ {print $5}')
        echo "The subdomain $subdomain is a $record_type record and points to $destination"
    else
        display_subdomain_info "$domain" "www"
        display_subdomain_info "$domain" "lb-01"
        display_subdomain_info "$domain" "web-01"
        display_subdomain_info "$domain" "web-02"
    fi
}

display_subdomain_info "$1" "$2"
